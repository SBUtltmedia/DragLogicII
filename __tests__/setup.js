// Setup file for Jest tests
// Simple node environment setup without requiring jsdom

// Since this is a basic setup, we can just leave it empty
// or add minimal mocking if needed
console.log('Jest setup completed');
